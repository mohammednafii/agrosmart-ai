# AgroSmart — FastAPI backend dependencies
# Install with:  pip install -r requirements.txt

# ── API framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Geospatial / raster I/O ────────────────────────────────────────────────────
rasterio>=1.3.0
numpy>=1.24.0

# ── Image rendering (transparent PNG export) ───────────────────────────────────
Pillow>=10.0.0

# ── Deep learning runtime — choose ONE of the following two blocks ─────────────

# Option A: TensorFlow 2.x (includes tf.keras)
#   pip install tensorflow>=2.13.0
tensorflow>=2.13.0

# Option B: Standalone Keras 3.x + TensorFlow backend
#   Uncomment lines below and comment out the tensorflow line above
#   keras>=3.0.0
#   tensorflow>=2.15.0   # required as Keras 3 backend

# ── HTTP client (for Clerk user sync and health-check tests) ───────────────────
httpx>=0.27.0

# ── Authentication (Clerk JWT verification) ────────────────────────────────────
PyJWT>=2.8.0
cryptography>=41.0.0   # required by PyJWT for RS256 algorithm

# ── Database (User persistence) ────────────────────────────────────────────────
sqlalchemy>=2.0.0      # ORM — SQLite by default, swap DATABASE_URL for Postgres

# ── Environment variables ──────────────────────────────────────────────────────
python-dotenv>=1.0.0
